# docker-laravel-lemp-phpmyadmin

- `docker-compose run --rm composer update`
- `docker-compose run --rm npm run dev`
- `docker-compose run --rm artisan migrate`
- `docker-compose -f docker-compose.yml up phpmyadmin`
